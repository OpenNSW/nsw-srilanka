apiVersion: v1
kind: Service
metadata:
  name: {{ include "lk-tnsw.backend.fullname" . }}
  labels:
    {{- include "lk-tnsw.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type | default "ClusterIP" }}
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "lk-tnsw.backend.selectorLabels" . | nindent 4 }}
